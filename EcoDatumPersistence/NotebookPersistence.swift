//
//  NotebookEntity+Model.swift
//  EcoDatumPersistence
//
//  Created by Kenneth Wingerden on 3/18/19.
//  Copyright © 2019 Kenneth Wingerden. All rights reserved.
//

import EcoDatumModel
import EcoDatumCoreData
import Foundation

class NotebookPersistence {
    
    
    
}

extension NotebookEntity {
    
    /*
    func toModel() throws -> Notebook {
        guard let id = id,
            let name = name,
            let createdDate = createdDate,
            let updatedDate = updatedDate else {
             throw PersistenceManager.PeristenceError.InvalidNotebook
        }
        
        let siteModels: [Site] = []
        if let sites = sites {
            for s in sites {
                let site = s as! SiteEntity
                siteModels.append(site.toModel())
            }
        }
        
        return Notebook(id: id,
                        name: name,
                        createdDate: createdDate,
                        updatedDate: updatedDate,
                        sites: )
    }
 */
    
}

extension Notebook {
    
    
}
